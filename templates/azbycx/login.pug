extends layout

block content

	style.
		body{
		background-color: #05AD9B;
		}
		.row{
			margin-top: 70px;
			padding: 5px;
		}
		.btn{
			color: #05AD9B;
		}
		label{
			color: white;
		}
		#reg{
			color:white;
			margin-left: 50px
		}
	body
		.container
			.row
				.col-lg-4

				.col-lg-4
					!= messages('message', locals)
					if errors
						each error,  i in errors
							p.alert.alert-success #{error.msg}

					h1.text-center(style='color:white') Login.
					p.alert-success #{info}

					form(method="POST", action="/medusa123/login")
						#form-group
							label Username
							input.form-control(name='username', type='text' required placeholder='Enter username..')
						#form-group
							label Password
							input.form-control(name='password', type='password' required placeholder='Enter Password..')
							br
						input.btn.btn-default(type='submit', value='Login')
						input(type='hidden' name='_csrf' value=csrfToken)
						a(href='/medusa123/register' id='reg') Don't have an account? Register

		
